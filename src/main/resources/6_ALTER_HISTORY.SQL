USE KURSOVOY;

ALTER TABLE USER_IP_HISTORY
CHANGE COLUMN USER_IP_HISTORY_ID USER_IP_HISTORY_ID INT(11) NOT NULL AUTO_INCREMENT,
ADD COLUMN USER_AGENT VARCHAR(512) NULL AFTER LOGIN_TIME,
ADD COLUMN LOCATION VARCHAR(45) NULL AFTER USER_AGENT;
